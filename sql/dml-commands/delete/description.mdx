---
title: Description
---

The `DELETE` command removes rows from a specified table. The deletion can be specific by using the `WHERE`
clause to identify the needed rows to delete. Without the `WHERE` clause, the command removes all the rows in
the table and leaves it empty.