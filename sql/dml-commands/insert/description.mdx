---
title: Description
---

The `INSERT` command inserts one or more rows into an existing table. The values inserted into each column
in the table can either be explicitly-specified using the `VALUES` keyword, or use the result of a query.