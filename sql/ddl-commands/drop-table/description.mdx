---
title: Description
---

The `DROP TABLE` command is used to remove a table from the current database (and in the future the current
schema), including all its rows, its metadata and related indexes.